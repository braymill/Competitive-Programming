#include <iostream>
#include <algorithm>
#define ll long long
using namespace std;

int main() {
	int T;
	ll n, k;
	cin >> T;
	for (int i = 0; i < T; i++) {
		cin >> n >> k;
		if (k == 1 && n % 2 == 1) {
			cout << "YES\n";
		}
		else {
			if (n % 2 == 1) {
				
			}
		}
	}
}